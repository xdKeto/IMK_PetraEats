<?php

class DetailTransaksi{
    private $ID_Transaksi;
    private $ID_Menu;
    private $kuantitas;
    private $subTotalHarga;

    function setKuantitas($Kuantitas){

    }

    function getKuantitas(){

    }

    function setSubTotalHarga($SubTotalHarga){

    }

    function getDetailTransaksi($ID_Transaksi) {
        // $sql = "SELECT * FROM DetailTransaksi WHERE id_transaksi=?";
        // $stmt = $pdo->prepare($sql);
        // $stmt->execute([$id]);
        // $rowCount = $stmt->rowCount();
        // $result = $stmt->fetch(PDO::FETCH_ASSOC);

        // if ($rowCount > 0) {
        //     return $result;
        // }
    }
   
}

?>